## Customize the test machine
machine:

  timezone:
    Asia/Tokyo

dependencies:
  cache_directories:
    - ~/cache_schema

test:
  override:
    - echo "----- No executable test -----"

deployment:
  travis_kick:
    branch: master
    commands:
      - echo "--- schema cache ---"
      - ./check_cache.sh
      - ./copy_cache.sh
#      - echo "----- travis kick -----"
#      - ./kick.sh
