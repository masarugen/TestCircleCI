## Customize the test machine
machine:

  timezone:
    Asia/Tokyo

#dependencies:
#  cache_directories:
#    - ~/cache_schema
#  override:
#    - ./copy_cache.sh

test:
  override:
    - echo "----- No executable test -----"
    - ls -l

deployment:
  travis_kick:
    branch: master
    commands:
#      - echo "--- schema cache ---"
#      - ./check_cache.sh
      - echo "----- travis kick -----"
      - ./kick.sh
