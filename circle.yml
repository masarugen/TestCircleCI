## Customize the test machine
machine:

  timezone:
    Asia/Tokyo

test:
  override:
    - echo "----- No executable test -----"

deployment:
  travis_kick:
    branch: master
    commands:
      - echo "----- travis kick -----"
      - ./kick.sh
